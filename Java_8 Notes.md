# Java 8 Notes

## Lambda Expressions.

These are expressions (similar to arrow functions in javascript) which can be used to write an implementation of an abstract class or an interface without creating a class for implementation. We can implement the unimplemented methods using lambda expressions.